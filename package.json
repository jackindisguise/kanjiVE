{
  "name": "kanjive",
  "version": "1.0.0",
  "description": "Collection of visual elements of Joyou kanji.",
  "main": "index.js",
  "scripts": {
    "compile": "node kanjivg2json && node prepare && node kanjive",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "colors": "^1.4.0"
  }
}
